pipeline {
   agent any
   stages {
     #stage name will be shown in the Jenkins UI
     stage("HELLO!\n Please read the logs and analyze them before moving on.") {
        steps { 
           echo "Hello! Thanks for visiting the Jenkins pipeline"
        } 
     }
   }
}
